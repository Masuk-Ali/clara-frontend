/**
 * Custom Hooks Index
 * Central export file for all custom hooks
 * 
 * Usage:
 *   import { useDataLoader, usePaginatedItems, useSearch } from '@/hooks';
 *   import { useTabs, useLocalStorage, useEscapeKey } from '@/hooks';
 */

export {
  useDataLoader,
  usePaginatedItems,
  useSearch,
  useTabs,
  useLocalStorage,
  useEscapeKey,
  useEnterKey,
  useDebounce,
  useIsMounted,
  useScore,
  useForm,
} from './index.js';
